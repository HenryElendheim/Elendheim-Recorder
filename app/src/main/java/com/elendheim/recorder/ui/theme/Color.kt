package com.elendheim.recorder.ui.theme

import androidx.compose.ui.graphics.Color

// Palette from the build plan. The splash and the app share the same tokens
// so the whole thing feels of a piece.
val RecorderBackground = Color(0xFF2B2B2B)   // dark gray (matches brand)
val RecorderSurface = Color(0xFF333333)      // slightly lifted panels
val RecorderAccent = Color(0xFFE57373)       // soft red (record button, highlights)
val RecorderText = Color(0xFFEDEDED)         // near-white body text on dark
val RecorderTextDim = Color(0xFF9A9A9A)      // secondary text
